post_install() {
  echo "==> Ghostty configuration has been installed to /etc/skel/.config/ghostty"
  echo "==> To use it, copy it to your home directory:"
  echo "    cp -r /etc/skel/.config/ghostty ~/.config/"
}
